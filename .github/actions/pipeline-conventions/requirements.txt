# The checker's only dependency. A manifest rather than an inline `pip install` in the Dockerfile,
# because Dependabot's pip ecosystem parses manifests and cannot see a version pinned in a RUN line.
pyyaml==6.0.3
